server:
  port: 8081

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://localhost:3306/auth_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

logging:
  level:
    org.springframework.security: INFO

hireconnect:
  jwt:
    issuer: hireconnect
    secret: "bXktc3VwZXItbG9uZy1zZWNyZXQta2V5LWZvci1kZW1vLWNvbnRyb2w="
    access-token-minutes: 15
    refresh-token-days: 7

